// ITS JUST A FILE TO TEST SOME FUNCTIONS


/*
function getRandom(min, max){
    min = Math.ceil(min);
    max = Math.floor(max);
    return Math.floor(Math.random() * (max-min)) + min;
}

function sortFish(){
    let fishes = [];
    fishes = ["Tilápia", "Tambaqui", "Traíra", "Piau", 
      "Mandi", "Bagre", "Bonito","Cachara","Piapara", "Piaba", "Pirarucu",
      "Cachorra", "Piranha", "Carpa", "Corvina", "Dourado", "Robalo", "Curimatã",
      "Surubim", "Raia", "Sardinha","Salmão", "Tambacu", "Tucunaré"];

    //let sortedFish = fishes.sort();
    //return sortedFish;
    //let randomNum = getRandom(1, fishes.lenght);
    //return fishes[randomNum].toString;

    min = Math.ceil(1);
    max = Math.floor(fishes.length);
    let sorted = Math.floor(Math.random() * (max-min)) + min;
    return fishes[sorted];
}

console.log(sortFish());*/




/*
const commands = require('./commands.json');

function list_commands_function(){
  //get an array from "command_list" json key
  list_commands = commands['command_list'];
  return list_commands.join(); // [ '!x', '!y', '!z' ]
}

console.log(list_commands_function())
*/


